import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'u19028386_Home_A_02',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
